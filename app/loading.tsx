import { LoadingSplash } from "@/components/ui/LoadingSplash";

/** Route-level loading UI (App Router). Shows the branded splash. */
export default function Loading() {
  return <LoadingSplash />;
}
